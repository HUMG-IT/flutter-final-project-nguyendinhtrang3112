import 'package:flutter/material.dart';

class AppColors {
  // Màu chính của ứng dụng (Primary Brand Color)
  static const Color primary = Color(0xFF6200EE); // Màu tím Google Material
  static const Color accent = Color(0xFF03DAC6); // Màu xanh ngọc tạo điểm nhấn

  // Màu trạng thái (Dùng để tô màu điểm số)
  static const Color passed = Color(0xFF4CAF50); // Xanh lá (Qua môn)
  static const Color failed = Color(0xFFF44336); // Đỏ (Trượt)
  static const Color warning = Color(0xFFFFC107); // Vàng (Cảnh báo/Điểm thấp)

  // Màu nền và chữ
  static const Color background = Color(0xFFF5F5F5); // Xám rất nhạt
  static const Color cardColor = Colors.white;
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.black54;
}
